/**
 * On press of button,
 * clear all drives of all content
 * (drive delete via api)
 */

export function NukeButton() {
    return (
        <div id="nuke_box">
            <span id="nuke_txt">Clean</span>
        </div>
    )
}